#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
#include<string.h>
#include <stdlib.h>
char word[500005][25];
char word2[500005][25];
int main()
{
	int N, M;
	scanf("%d %d", N, M);
	for (int i = 0; i <  N; i++)
	{
		scanf(" %s", word[i]);

	}
	for (int i = 0; i < M; i++)
	{
		scanf(" %s", word2[i]);
	}
	for()
}